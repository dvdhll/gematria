# מחשבון גימטרייה

אפליקציית PWA (RTL, עברית) לחישובי גימטרייה מתקדמים + חיפוש הפוך בתנ״ך.
מבוססת על הפרק "מבוא לגימטריא" מתוך הספר *עיניך ברכות בחשבון* (הרב יצחק).

## מה יש בפנים

**מנוע חישוב** (`js/gematria.js`, ללא תלויות, 61 בדיקות עוברות מול הדוגמאות שבמסמך):
- ערכי אות: הכרחי · סידורי · קטן · קדמי (+ סופיות רגילות/גדולות 500–900).
- ערכי מילה: הכרחי · גדול · סידורי · קטן · קדמי · קטן מספרי · קטן מספרי אחרון (+ שני, לפי חצאי פסוק) · מילוי/שמי · עם הכולל · מוסף · מרובע כללי · מרובע פרטי.
- פעולות: **הכאה** (מכפלת אותיות) · **הכאה פרטית** (מכפלה סקלרית אות-מול-אות).
- **ממוצע וכנפיים** · **נקודה אמצעית / שלם וחצי**.
- **מספרים צורניים** (זיהוי + יצירה, כולל שני אופני הסימון "של n" מול "ה-n-י"):
  משולש · מרובע · השראה · יהלם · חוה · ברית · חשמל · בית · שבת · מגן דוד · מבוקע · טטרהדרל.
- **סדרות**: חתך זהב `gs(n)` · שחזור סדרה חיבורית · שיטת ההפרשים · פיבונאצ׳י.

**חיפוש הפוך בתנ״ך** (`js/search.js` + `data/verses.json`):
- 23,204 פסוקים · 56,069 מילים ייחודיות (מקור: Sefaria, ניקוד/טעמים הוסרו).
- "מצא מילים/פסוקים ששווים לערך X" בשיטת הכרחי/סידורי/קטן/קדמי.

**PWA**: `manifest.json` + `sw.js` (עבודה אופליין, כולל המאגר).

## מבנה
```
index.html            · שלד הממשק
css/styles.css        · עיצוב (חם/מסורתי — קלף, כחול-נייבי, זהב)
js/gematria.js        · מנוע החישוב (טהור, גם ל-Node)
js/search.js          · חיפוש במאגר
js/ui.js              · קישור הממשק
data/verses.json      · מאגר התנ"ך (~3.6MB)
data/build_corpus.js  · סקריפט בניית המאגר (הרצה חוזרת: node data/build_corpus.js)
tests/test.js         · בדיקות מול המסמך (node tests/test.js)
server.js             · שרת סטטי לפיתוח מקומי
manifest.json, sw.js, _headers
```

## הרצה מקומית
```
node server.js           # http://localhost:8891
node tests/test.js       # בדיקות המנוע
```

## דיפ-לינק
- `?q=בראשית` — טעינה עם טקסט מוכן.
- `#search` / `#figurate` / `#series` / `#ops` — פתיחת טאב.

## פריסה ל-Cloudflare Pages
אתר סטטי לחלוטין. פריסה בפקודה אחת:
```
npx wrangler pages deploy . --project-name gematria
```
או חיבור ל-Git ב-Cloudflare Pages (Build command: ריק, Output dir: `/`).

## TODO / רעיונות להמשך
- מילוי: תמיכה בכל צורות המילוי (ביו"ד/בה"א/באל"ף) כבורר.
- מספר מספרי (ערך *שם* המספר, עשר=570) — דורש מנוע מספר→מילים עברי.
- קטן מספרי שני: זיהוי אוטומטי של האתנחתא (כרגע פיצול ידני/אמצעי).
- חיפוש: מילים "עם הכולל", טווח ערכים, ומיון תוצאות פסוקים.
- אייקון PWA כקובץ PNG (כרגע SVG data-uri).
